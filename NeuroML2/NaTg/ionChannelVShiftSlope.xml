<Lems xmlns="http://www.neuroml.org/lems/0.7.6"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.neuroml.org/lems/0.7.6 ../../LEMS/Schemas/LEMS/LEMS_v0.7.6.xsd"
      description="Defines a new channel type with a shift and slope variable for both m and h rate variables">

    <ComponentType name="ionChannelVShiftSlope"
                   extends="ionChannelVShift"
                   description="Same as _ionChannel_, but with _vShift and _slope parameters to change voltage activation of gates. The exact usage of _vShift and _slope in expressions for rates is determined by the individual gates.">

        <Parameter name="vShift_m" dimension="voltage"/>
        <Parameter name="slope_m" dimension="none"/>
        <Parameter name="vShift_h" dimension="voltage"/>
        <Parameter name="slope_h" dimension="none"/>
        <!-- TODO: Ensure Text elements are inherited from base type -->
        <Text name="species"/>

    </ComponentType>

    <ComponentType name="channelDensityVShiftSlope"
        extends="channelDensity"
        description="Same as _channelDensity_, but with _vShift and _slope parameter to change voltage activation of gates. The exact usage of _vShift in expressions for rates is determined by the individual gates.">

        <Parameter name="vShift_m" dimension="voltage"/>
        <Parameter name="slope_m" dimension="none"/>
        <Parameter name="vShift_h" dimension="voltage"/>
        <Parameter name="slope_h" dimension="none"/>

        <!-- TODO: Ensure Text elements are inherited from base type -->
        <Text name="segmentGroup" description="Which _segmentGroup_ the channelDensity is placed on. If this is missing, it implies it is placed on all _segment_s of the _cell_"/>
        <Text name="ion" description="Which ion flows through the channel. Note: ideally this needs to be a property of ionChannel only, but it's here as it makes it easier to select channelPopulations transmitting specific ions."/>


        <Structure>
            <ChildInstance component="ionChannel"/>
        </Structure>

    </ComponentType>

</Lems>
